# TASKS.md

交互文件 —— 复杂需求写在这里，Claude 会自动读取并执行。

---

## 使用方式

在对话中提到 **"交互文件"**、**"TASKS"**、**"任务文件"**、**"需求文件"** 或 **"todo"** 时，Claude 会自动来这里找活干。

下面按区块写需求，完成后在标题后标 `<!-- ✓ -->`。

---

<!-- 在下面写需求，每个需求一个区块 -->

